/** How long a cart hold lasts before the unit is released (ms). */
export const RESERVATION_TTL_MS = 30 * 60 * 1000

/** Extra hold while customer is on checkout / Razorpay (ms). */
export const CHECKOUT_RESERVATION_TTL_MS = 45 * 60 * 1000
